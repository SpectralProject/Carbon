#[test]
fn test_basics() {
    // open an iced-rs window

    // render the console with an expanding text box and vertically scrollable view
    // no horizontal scroll, lines are wrapped at 80 characters. Encourage use of new lines

    // make background color black for the console view area and the entire window

}
